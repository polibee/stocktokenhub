{
  "name": "stocktokenhub",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "docusaurus": "docusaurus",
    "start": "docusaurus start",
    "build": "docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids",
    "typecheck": "tsc",
    "seo:submit": "node scripts/submit-to-search-engines.js",
    "seo:robots": "node scripts/generate-robots.js",
    "build:seo": "npm run build && npm run seo:robots && npm run seo:submit",
    "analyze": "npm run build && npx webpack-bundle-analyzer build/static/js/*.js",
    "check:deployment": "node scripts/check-deployment.js",
    "deploy:check": "npm run build && npm run serve & sleep 5 && npm run check:deployment"
  },
  "dependencies": {
    "@docusaurus/core": "3.8.1",
    "@docusaurus/preset-classic": "3.8.1",
    "@docusaurus/plugin-google-gtag": "3.8.1",
    "@docusaurus/plugin-sitemap": "3.8.1",
    "@mdx-js/react": "^3.0.0",
    "axios": "^1.10.0",
    "cheerio": "^1.1.0",
    "clsx": "^2.0.0",
    "date-fns": "^4.1.0",
    "fs-extra": "^11.3.0",
    "prism-react-renderer": "^2.3.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "3.8.1",
    "@docusaurus/tsconfig": "3.8.1",
    "@docusaurus/types": "3.8.1",
    "typescript": "~5.6.2"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=18.0"
  }
}
